// import TrackReel from '../../Models/trackReelModel.js';

// export const UpdateAllReels = async () => {
// 	const reel = await TrackReel.find({});
// 	if (reel.length != 0) {
// 		const time = Date.now();
// 		for (const reel of await TrackReel.find({
// 			status: 'pending',
// 		})) {
// 			if (
// 				reel.iterationsCompleted <=
// 				reel.totalIterations
// 			) {

// 			}
// 		}
// 	} else {
// 		console.log('No reels found');
// 		return;
// 	}
// };

// UpdateAllReels();
